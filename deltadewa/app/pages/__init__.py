"""Page layouts for the Dash app: ``/monitor`` and ``/design``.

Each module exports a module-level ``layout``. Routing between them lives in
``deltadewa.app.factory``, not here — a page module only builds its own
content.
"""
